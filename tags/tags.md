## Tags
>Suelen seguir semantic versioning de sintaxis

git tag 1.0.0 (*crear local*)

git push --tags (*dos guiones, crear total*)

git checkout 1.0.0

git tag -d 1.0.0 (*borrar local*)

git push --delete origin 1.0.0 (*dos guiones, borrar total*)
